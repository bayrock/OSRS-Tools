function calculatePetChance() {
    // return the chance to the end user
}

function addSelections() {
    // add the patches to the selection list
}

$.getJSON("patches.json", (data) => {
    console.log(data)
})